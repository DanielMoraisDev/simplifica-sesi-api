const mongoose = require("mongoose")
const RepresentanteModel = require('../../models/Representantes.js')

const searchByIDRepresentante = async (req, res, id, model) => {
    
}

module.exports = {
    searchByIDRepresentante
}